// OneSignal Web SDK service worker (v16).
// Kept in its own scope (/onesignal/) so it never conflicts with the
// Workbox/PWA service worker registered at the root scope.
importScripts("https://cdn.onesignal.com/sdks/web/v16/OneSignalSDK.sw.js");
